{
    "name": "empstat-workspace",
    // "dockerComposeFile": "docker-compose-dev.yml",
    // "service": "devcontainer",
    // "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}"

    "build": {
        "dockerfile": "dockerfile"
    }
}
